<h3>Patients list</h3>
<ul>
  <% @patients.each do |patient| %>
    <li style = "font-weight: bold;"><%= link_to "Name: #{patient.name} Number of Appointments: #{patient.appointments.count}", patient_path(patient.id) %></li>
  <% end %>
</ul>
